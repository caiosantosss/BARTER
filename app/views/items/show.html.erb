<div class = "container">
  <div class="card" style = "background-image:url(<%= @item.photo.attached? ? cl_image_path(@item.photo.key) : "" %>)">
  <h1><%= @item.name %></h1>
  <h3><%= @item.description %></h3>
  <%= link_to 'Offer', new_item_offering_path %>
</div>
</div>
