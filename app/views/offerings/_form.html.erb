<%= simple_form_for [@item, @offering] do |f| %>
  <%= f.input_field :offered_id,
  collection: @user.items.map {|item| item.name },
  include_blank: '(All)',
  selected: [''],
  as: :check_boxes %>
  <%=f.button :submit, label: 'Confirm' %>
<% end %>

<%# :collection =>
  InspirationImage.all.map { |img| [image_tag(img.image.url(:thumb)).html_safe, img.id] } %>

<%# cl_image_tag(@user.avatar.key, width: 300, height: 200, crop: :scale, effect: "cartoonify") %>

<%# @user.items.map {|item| [cl_image_tag(item.photo.key, width: 300, height: 200, crop: :scale)] } %>
